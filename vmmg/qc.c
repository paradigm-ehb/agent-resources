#include "../arena.h"
#include "../base.h"
/* b32
#include <curl/easy.h>
#include <libvirt/libvirt.h>
#include <curl/curl.h>
#include <libssh/libssh.h>

 download_iso(char *path)
 {
   CURL *curl;
   CURLcode result = curl_global_init(CURL_GLOBAL_ALL);

   if (!result)
   {
     return (int)result;
   }

   curl = curl_easy_init();

   if (curl)
   {
     curl_easy_setopt(handle, opt, param)
   }

   return 0;
}
*/
